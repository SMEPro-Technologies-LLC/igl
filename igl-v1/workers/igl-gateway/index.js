/* Deploy entry for the governed-decode gateway. One implementation, re-exported,
   same pattern as igl-api. Wrangler bundles the src graph. */
export { default } from "../../src/gateway-worker.js";
